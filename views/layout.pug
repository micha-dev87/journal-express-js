doctype html
html(lang="fr")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title MindBox - #{title || 'Journal Personnel'}
    link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css")
    link(rel="stylesheet", href="/style.css")
    
  body
    header
      nav.navbar.navbar-expand-lg.navbar-light
        if user
          a.nav-link(href="/journal") Mon Journal
          a.nav-link(href="/logout") Déconnexion
        else
          a.nav-link(href="/auth") Connexion
          a.nav-link(href="/register") Inscription
    
    main.container
      block content
    
    footer.text-center.mt-4
      p &copy; #{new Date().getFullYear()} MindBox - Journal Personnel 
      script(src="https://code.jquery.com/jquery-3.5.1.slim.min.js")
      script(src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js")
      script(src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js")